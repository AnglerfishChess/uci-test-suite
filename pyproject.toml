[project]
name = "uci-test-suite"
version = "0.3.0"
description = "A Python-based set of tests for UCI server/chess engine implementations"
authors = [
    {name = "Alex Myodov", email = "amyodov@gmail.com"}
]
license = {text = "MIT License"}
readme = {file = "README.md", content-type = "text/markdown"}
keywords = ["chess", "UCI", "chess engine", "testing", "test suite", "uci protocol", "uci chess"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Topic :: Games/Entertainment :: Board Games",
    "Topic :: Software Development :: Testing",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.13",
    "Operating System :: OS Independent"
]
requires-python = ">=3.13"
dependencies = [
    "click>=8.1.8,<9.0.0",
    "esca>=0.2.1,<0.3",
]

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "pyrefly>=0.16.0",
    "ruff>=0.11.4",
]

[project.scripts]
uci-test-suite = "uci_test_suite.__main__:main"

[tool.setuptools]
package-dir = {"" = "."}
packages = ["uci_test_suite", "uci_test_suite.checks"]

[tool.ruff]
line-length = 120
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "PT", "RUF"]

[tool.ruff.lint.per-file-ignores]
# Parametrized over many differently-invalid inputs, each raising a differently-worded ValueError;
# the message content is already pinned by the dedicated tests below. A `match` here would either
# be too narrow (fail on most cases) or too loose to mean anything.
"tests/test_levels.py" = ["PT011"]

[tool.pyrefly]
project-includes = ["uci_test_suite", "tests"]
search-path = [".", "tests"]
python-version = "3.13"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--strict-markers -ra"
markers = [
    "stockfish: integration test requiring the stockfish binary on PATH",
]
